name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Package
        run: |
          ./scripts/package-macos-release.sh --output-dir dist
          cp target/release/topside dist/topside
          tar -czf dist/topside-macos-universal.tar.gz -C dist topside

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: topside-macos
          path: |
            dist/topside-macos-universal.tar.gz
            dist/topside-macos.dmg

  publish:
    needs: build-macos
    runs-on: ubuntu-latest
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: topside-macos
          path: dist

      - name: Publish GitHub Release assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/topside-macos-universal.tar.gz
            dist/topside-macos.dmg
